<%= form_for(@user, html: {class: "nice"}) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'input-text' %><br />
  </div>
  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email, class: 'input-text' %><br />
  </div>
  <div class="actions">
    <%= f.submit class: 'small round button' %>
  </div>
<% end %>
