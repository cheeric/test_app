<%= form_for(@product, html: {class: "nice"}) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'input-text' %><br />
  </div>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description, class: 'input-text' %><br />
  </div>
  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price, class: 'input-text' %><br />
  </div>
  <div class="actions">
    <%= f.submit class: 'small round button' %>
  </div>
<% end %>
